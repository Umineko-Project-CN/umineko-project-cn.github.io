---
title: 帮助
description: 海猫鸣泣之时汉化版帮助中心 - 提供PS3版移植版和NS版汉化补丁常见问题解答和常用技巧。
---
import Icon from '@components/Icon.astro';
import Kbd from "@components/Kbd.astro"
import Accordion from "@components/Accordion.astro"
import AccordionItem from "@components/AccordionItem.astro"
import Heading from "@components/Heading.astro"
export const components = {
    h2: (props) => <Heading {...props} tag="h2" />,
    h3: (props) => <Heading {...props} tag="h3" />,
    h4: (props) => <Heading {...props} tag="h4" />,
    blockquote: (props) => <Heading {...props} tag="blockquote" />,
};

## 版本对比
- PS3 版移植版与 NS 版汉化补丁对比如下：

    |  | 《海猫鸣泣之时 ～轮舞曲与夜想曲 移植版～》 | 《海猫鸣泣之时咲 ～猫箱与梦想的交响曲～》汉化补丁 |
    | --- | --- | --- |
    | 性质 | 基于 PS3 版游戏移植重制的游戏 | 适用于 NS 游戏的汉化 MOD |
    | 支持平台 | 支持 Windows、macOS、Linux<br/>也可在 Android、iOS 运行 | 支持 NS 模拟器（Windows、macOS、Android 等）<br/>或已破解 NS 主机 |
    | 收录剧本 | 《海猫鸣泣之时》（正篇 Episode1-4）<br/>《海猫鸣泣之时散》（正篇 Episode5-8） | 《海猫鸣泣之时》（正篇 Episode1-4）<br/>《海猫鸣泣之时散》（正篇 Episode5-8）<br/>《海猫鸣泣之时翼》（番外）<br/>《海猫鸣泣之时羽》（番外）<br/>《海猫鸣泣之时咲》（番外）<br/>龙骑士07撰写的主机版独占游戏剧本（番外） |
    | 译文升级 | 包含 | 包含 |
    | 错误修正 | 包含 | 包含 |
    | 文本·语音改动还原 | 包含 | “基础版”汉化补丁不包含<br/>“还原版”汉化补丁包含 |
    | BGM 改动还原 | 不需要 |  “基础版”汉化补丁不包含<br/>“还原版”汉化补丁包含 |

- 译文升级：译文经过进一步的人工校对润色，更加准确与通顺。
- 错误修正：修正了多处主机版错置的图片·语音·特效，力求呈现更加完美。
- 改动还原：主机版《海猫》因分级与版权等原因，对部分文本·语音·BGM进行了改动。“还原”指贴近改动前的原版。

## PS3 版移植版帮助

### 游戏贴士

#### 如何操作

<div class="[&_th]:w-1/2">
- **键盘与鼠标：**

    | 快捷键 | 功能 |
    | --- | --- |
    | <Kbd>A</Kbd> | 自动播放 |
    | <Kbd>F</Kbd> | 全屏/窗口模式切换 |
    | <Kbd>Ctrl</Kbd>+<Kbd>S</Kbd> / <Kbd>Alt</Kbd>+<Kbd>S</Kbd> | 文本快进 |
    | <Kbd>Alt</Kbd>+<Kbd>M</Kbd> | 静音/取消静音 |
    | 鼠标右键 | 打开/关闭菜单 |
    | 鼠标滚轮 | 打开“剧情回顾”界面 |

- **触摸控制：**

    | 操作 | 功能 |
    | --- | --- |
    | 单指点击 | 左键单击 |
    | 双指点击 | 右键单击 |
    | 三指点击 | 中键单击 |
    | 双指滑动 | 滚动 |
    | 三指向右滑动 | 跳过文本 |
    | 三指向左滑动 | 自动模式 |
    | 三指向上滑动 | 静音/取消静音 |
    | 三指向下滑动 | 标签页（跳至下一章节） |

- **游戏手柄：**

    - **支持手柄：**游戏原生支持 **DualShock 3/4、Steam Controller** 和 **FuSa 手柄**。

    - **其他手柄：**需要手动配置按键映射。请在 `ons.cfg` 配置文件中加入 `pad-map` 代码。

    - **Xbox 手柄参考代码：**

        ```ini
        pad-map=3,1,0,2,8,4,18,19,20,21,9,-1,-1,-1,-1,5,-1,-1
        ```

        映射：`返回, 确认, 取消, 菜单, 自动, 快进, 下, 左, 上, 右, 静音, 未知, 未知, 未知, 按住快进, 未知, 未知`

    - **自定义方法：**

        1. 在 `ons.cfg` 配置文件中加入 `use-consolehelp/help_read4` 和 `debug` 两行。

        2. 启动游戏，此时会弹出控制台窗口。

        3. 在游戏内按下手柄按键，控制台会显示对应的按键数字。

        4. 按照映射顺序，将记录下的数字填入 `pad-map=` 的等号后（使用 `-1` 代表不映射该按键）。
</div>

****
#### 查看注释

- 当游戏中出现绿色文本时，表示该词条拥有注释。

    ![](@assets/pages/help/help_read1.webp)

- 此时，可以打开游戏菜单（PC 端为鼠标右键），点击左上角的“TIPS”按钮，进入“情报录”界面。

    ![](@assets/pages/help/help_read2.webp)

- 接着，点击左下角的“切换为魔导书”，即可查阅相关注释。部分条目包含多条注释，可上下滚动查看。

****
#### 查看彩蛋

- 游戏中存在部分由制作组制作的隐藏内容，仅提供两条线索供诸位探索：

    1. 若想要初探隐秘之境，首先需要提高“版权保护”意识。

    2. 若想要在与魔女的战斗中取胜，必须先得到黄金的真实。

****
#### 存档文件的路径

- **Windows Vista 或更高版本：**

    - `%SYSTEMDRIVE%\ProgramData\ONScripter-RU\UminekoPS3Fication**\`

- **Windows XP：**

    - `%SYSTEMDRIVE%\Documents and Settings\All Users\Application Data\ONScripter-RU\UminekoPS3Fication**\`

- **Windows 平台下启用 iCloud Drive App 情况下：**

    - `iCloud Drive\ONScripter-RU\UminekoPS3Fication**\`

- **macOS (10.7 或更高版本，iCloud)：**

    - `~/Library/Mobile Documents/com-apple-CloudDocs/ONScripter-RU/UminekoPS3Fication**/`

- **macOS (10.6 版本)：**

    - `~/Library/Application Support/ONScripter-RU/UminekoPS3Fication**/`

- **Linux：**

    - `~/.onscripter/UminekoPS3Fication**/`

- **注意：**

    - `**` 代表游戏语言，例如 `Cn`，`Cht`，`En` 等。

    - 不同版本的游戏（简体中文、繁体中文、英文、俄文版等）之间存档不共享。

    - 如需停止使用 iCloud，请在 `ons.cfg` 配置文件中另起一行，加入 `disable-icloud`。

****
#### 配置文件与命令行指令

- **常用参数一览：**

    | 参数 | 取值范围 | 说明 |
    | :--- | :--- | :--- |
    | `window-width` | 单位为宽度像素 | 自由设置窗口分辨率 |
    | `final-voicedelay-time` | 单位为毫秒 | 设置每句话后停顿时长 |
    | `cursor` | hide/show/auto | 控制隐藏鼠标 |
    | `env[complete_episode]` | 1-8 | 标记已完成的章节 |
    | `env[force_game]` | chiru/rondo | 强制指定游戏模式 |
    | `env[first_launch]` | passed/force | 控制首次启动流程 |
    | `env[cg_unlock]` | 1-8 | 解锁指定章节的CG |
    | `env[bgm_unlock]` | 1-8 | 解锁指定章节的BGM |
    | `env[automode_hide_cursor]` | true/false | 自动模式下隐藏光标 |
    | `env[bgm_title_language]` | English/Russian/Romaji/Japanese | BGM标题语言 |
    | `env[skip_title]` | all/force | 跳过标题画面 |
    | `env[video_resolution]` | 720/1080 | 视频分辨率 |
    | `env[control_interface]` | kb/pad | 控制界面类型 |
    | `env[object_caching]` | true/false | 对象缓存开关 |
    | `env[bgm_volume]` | 0-100 | BGM音量gl |
    | `env[effect_volume]` | 0-100 | 音效音量 |
    | `env[voice_volume]` | 0-100 | 语音音量 |
    | `env[rumble_strength]` | 0-100 | 震动强度 |
    | `env[text_speed]` | 0-10 | 文字显示速度 |
    | `env[textbox_type]` | N/B/L/T | 文本框类型 |
    | `env[lipsync]` | true/false | 唇形同步 |
    | `env[display_titles]` | true/false | 显示标题 |
    | `env[subtitles]` | Translation/Romaji/Both/None | 字幕设置 |
    | `env[skip_unread_dialogues]` | true/false | 跳过未读对话<br/>*需要 v3.3a 或更新版本* |
    | `env[wheel_down_hides_textbox]` | true/false | 滚轮下滚隐藏文本框<br/>*需要 v3.3a 或更新版本* |
    | `env[legacy_op]` | true/false | 使用旧版片头<br/>*需要 v4.1b 或更新版本* |
    | `env[steam_conventions]` | true/false | 使用 Steam 版假名修正<br/>*需要 v4.1b 或更新版本* |
    | `env[verify]` | quick/full/once/none | 文件验证模式<br/>*需要 v5.1b 或更新版本* |

- **查看所有可用参数：**

    ```bash
    ./onscripter-ru --help
    ```

- **配置示例：**

    ```ini
    # 在 ons.cfg 配置文件中设置
    env[force_game]=chiru
    env[effect_volume]=33
    env[text_speed]=5
    env[bgm_title_language]=Japanese
    ```

    ```bash
    # 或通过命令行传递
    ./onscripter-ru --env[force_game] chiru --env[effect_volume] 33 --env[bgm_title_language] Japanese
    ```

### 常见问题

#### 解压失败

- 若解压出错，通常是软件版本过旧或下载损坏。

    - 请使用 [7-Zip](https://www.7-zip.org/download.html)、[PeaZip](https://peazip.github.io/)、[WinRAR](https://www.rarlab.com/download.htm) 等常用压缩软件。

    - 请核对分包的 **SHA1 校验码**（在“1. 完整游戏”文件夹内的 `SHA1.txt` 中查看）。若不一致需重新下载该分包。

****
#### 启动器更新失败

- **Windows 7 更新失败：**

    - Windows 7 用户如果更新失败并提示 `System.IO.FileNotFoundException`，通常是因为缺少必要的系统组件。请下载安装 **[.NET Framework 4.8](https://dotnet.microsoft.com/download/dotnet-framework/thank-you/net48-offline-installer)**。

- **其他情况更新失败：**

    - 也可从“下载链接”的“**更新下载**”中，分别下载“脚本”与“资源包”，自行解压到游戏目录。

****
#### 游戏显示问题

- **PC 平台：**

    - **显示异常：**可能是因为显卡驱动程序已过时。本游戏引擎使用 OpenGL，过时的驱动可能导致问题。请尝试更新显卡驱动。

    - **显示不全：**可能是系统缩放导致的。请右键点击游戏主程序 `onscripter-ru.exe`，在“属性”→“兼容性”中，将高 DPI 缩放设置更改为“应用程序”。

- **Android 平台：**

    - **显示异常：**某些设备需要在 `ons.cfg` 配置文件中添加 `render-self=no` 以修复画面异常。

    - **显示不全：**需要将游戏主程序（.apk）更新到 r3569 或更高版本，并在 `ons.cfg` 配置文件中指定用于将游戏画面左移的手动屏幕偏移量。例如，对于屏幕分辨率为 2400x1080 的 Redmi Note 9 Pro，应添加 `system-offset-x=180`。

****
#### 游戏没有声音

- **检查静音状态：**查看游戏窗口标题栏是否显示 `[Sound: Off]`。如果是，请按下 <Kbd>Alt</Kbd>+<Kbd>M</Kbd> 取消静音。

- **检查角色语音：**进入游戏的环境设置（第三页），确保所有角色的语音都已开启。

- **修改音频设置：**如果上述方法无效，请尝试修改 `ons.cfg` 配置文件：

    - 添加 `audioformat=VALUE`，`VALUE` 可尝试 `s16` 或 `s32`。

    - 或者添加 `audiodriver=VALUE`，`VALUE` 可尝试 `directsound` 或 `wasapi`。

- **兼容性问题：**如果所有方法都无效，则可能是引擎与你的设备存在兼容性问题，暂时无法解决。

****
#### 游戏内字号过大

- 可以在 `ons.cfg` 配置文件中加入 `font-multiplier` 代码来缩放字号。将代码中所有的 `0.9` 替换为你希望的缩放比例。

    - **注意：**此功能可能导致文字在极少数情况下出现显示不稳定的问题。请谨慎使用。

- **代码示例（缩放为 0.9 倍）：**

  ```ini
  font-multiplier=b1:0.9,b2:0.9,b3:0.9,b4:0.9,b5:0.9,b6:0.9,b7:0.9,p0:6:0.9,p1:1:0.9,p1:2:0.9,p1:3:0.9,p1:4:0.9,p1:5:0.9,p1:7:0.9,p2:1:0.9,p2:2:0.9,p2:3:0.9,p2:4:0.9,p2:5:0.9,p2:7:0.9,p41:1:0.9,p41:2:0.9,p41:3:0.9,p41:4:0.9,p41:5:0.9,p41:7:0.9,p42:1:0.9,p42:2:0.9,p42:3:0.9,p42:4:0.9,p42:5:0.9,p42:7:0.9
  ```

****
#### 存档、读档时出现问题

- 可以尝试在游戏的环境设置中选择“初始化游戏”来重置状态。如果问题依旧，可以手动删除存档文件夹来彻底解决。

    - **注意：**这将删除所有存档，操作前请先备份。

****
#### Android 平台运行卡顿

- 目前存在多种可能影响性能的问题，因此建议仔细配置以下选项：

    - **选择渲染器：**默认情况下，ONScripter-RU 使用 **OpenGL ES 2.0** 渲染器。如果初始化失败，或者 `ons.cfg` 配置文件中存在 `renderer-blacklist=GLES2` 这行设置，则会回退到 **OpenGL ES 3.0** 渲染器。某些设备可能不支持 OpenGL ES 3.0，但启用它可能会带来稍好的性能。

    - **启用硬件视频解码：**默认情况下，由于性能不佳，ONScripter-RU 在 Android 上禁用了硬件视频解码。但你可以通过在 `ons.cfg` 配置文件中添加 `hwdecoder=on` 来启用它，这可能有助于降低功耗。请注意，无论是否启用此选项，都无法保证在任何移动设备上都能流畅播放 1080p 视频。

    - **后备纹理加载：**在某些设备上使用 OpenGL ES 2.0 渲染器时，你可以通过启用额外的 RAM 缓存来改善特定纹理（例如配置菜单）的加载性能。为此，请在 `ons.cfg` 配置文件中添加 `texture-upload=ramcopy`。请注意，这可能导致内存不足的低端设备崩溃。使用 OpenGL ES 3.0 渲染器时，此设置将被忽略。

    - **帧率限制：**你可以通过将游戏帧率限制在低于 60 帧/秒的值来改善性能并显著降低功耗。请在 `ons.cfg` 配置文件中添加 `force-fps=30`，其中 30 是你期望的帧率值。你还可以额外添加 `force-vsync` 来启用旧式垂直同步，但这不太可能带来明显益处。不设置帧率限制可能导致在某些硬件上的部分场景中出现黑条等渲染异常。

    - **纹理尺寸限制：**通过将支持的纹理尺寸限制设置为低于设备本身支持的值，你可能会在某些场景中获得稍好的性能。请在 `ons.cfg` 配置文件中添加 `texlimit=4096`，其中 4096 是以像素为单位的 2 的幂次方值。

****
#### Linux 发行版不受支持

- 游戏在 **Wine** 环境下可以完美运行（经测试，Wine 1.7.21 下运行正常）。
- 建议使用 Wine 的 **1.6 分支或更新的最新版本**。

## NS 版汉化补丁帮助

### 游戏贴士

#### 如何操作

- 在标题界面或系统菜单选择“Manual”，可以打开说明书，在“操作方式”页查看游戏键位。
    - 若使用模拟器游玩，可以在模拟器的设置界面查看键位映射。

    ![](@assets/pages/help/help_read3.webp)

****
#### 跳过正篇

- 如果已经游玩过《海猫》的正篇剧本（EP1-8），可以在剧本选择界面按“+”键，解锁所有正篇剧本，直接开始阅读番外剧本。

    - 如果是初次游玩，不推荐直接解锁。

    ![](@assets/pages/help/help_read4.webp)
